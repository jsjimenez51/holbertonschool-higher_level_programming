#!/usr/bin/node
// prints 3 lines by using an array of strings and a loop

let strings = ['C is fun', 'Python is cool', 'Javascript is amazing'];

let idx = 0;
for (idx in strings) console.log(strings[idx]);
